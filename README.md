// ...existing code...

## 使用说明

在需要重命名文件的目录下运行:

```bash
rename-files
```

然后根据提示进行操作:

1. 选择目标命名格式(源格式将自动识别)
2. 确认是否递归处理子目录
3. 输入文件名前缀(可选)
4. 选择是否使用试运行模式

所有选项都可以通过方向键选择或直接输入。

// ...existing code...
